name: Norminette
run-name: ${{ github.actor }} is testing out Norminette 🐱
on: [push]

jobs:
  norminette:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: alexandregv/norminette-action@v3
